import { Avatar } from '../../components/common.jsx';
import { patients } from '../../data/caregiverData.js';

export default function JobsPage({ onOpenPatient, onNotify }) {
  return <section className="page-card panel"><div className="page-intro"><div><p className="eyebrow">04 active assignments</p><h2>My jobs</h2><p>See where you need to be and what each person needs today.</p></div><button className="primary compact" onClick={() => onNotify('Route optimized: 12.4 km · 34 min')}>Plan today’s route</button></div><div className="route"><div className="map route-map"><span className="map-label one">Taman Desa</span><span className="map-label two">Bangsar</span><span className="map-label three">Seputeh</span><div className="pin p1"><span>1</span></div><div className="pin p2"><span>2</span></div><div className="pin p3"><span>3</span></div></div><div className="route-summary"><h3>Today’s route</h3><strong>12.4 km</strong><p>Estimated travel time · 34 min</p><div className="route-stops"><span>Start</span><i className="stop-line" /><span>3 stops</span></div></div></div><div className="patient-list"><div className="patient-list-head"><span>Patient & location</span><span>Care plan</span><span>Visit time</span><span /></div>{Object.entries(patients).map(([id, patient]) => <button className="patient-row" key={id} onClick={() => onOpenPatient(id)}><span className="patient"><Avatar initials={patient.initials} tone={patient.tone} /><span><strong>{patient.name}</strong><small>{patient.location} · {patient.distance}</small></span></span><span className="care-type">{patient.care}</span><span className="time">{patient.time}</span><span className="arrow">›</span></button>)}</div></section>;
}
