import { Avatar } from '../../components/common.jsx';
import { patients, stats } from '../../data/caregiverData.js';

export default function DashboardPage({ onNavigate, onOpenPatient, onNotify }) {
  return <><section className="stats">{stats.map(([label, value, note, icon]) => <article className="stat" key={label}><div className="stat-head">{label}<span className="stat-icon">{icon}</span></div><h2>{value}</h2><small>{note}</small></article>)}</section><div className="grid"><section className="panel"><div className="head"><h3>Assigned jobs</h3><button className="link-button" onClick={() => onNavigate('jobs')}>View all</button></div>{Object.entries(patients).map(([id, patient]) => <button className="job" key={id} onClick={() => onOpenPatient(id)}><Avatar initials={patient.initials} tone={patient.tone} /><span><strong>{patient.name}</strong><small>{patient.care} · {patient.time}</small></span><span className="status">{id === 'mei' ? 'Next' : 'Upcoming'}</span></button>)}</section><section className="panel wellbeing"><div className="head"><h3>Today at a glance</h3><span className="good">On track</span></div><div className="ring"><strong>72%</strong><small>completed</small></div><p>Keep going, Sarah. Your afternoon visits are lined up and ready.</p><button className="outline" onClick={() => onNotify('Map view is ready for your active jobs')}>View on map <span>↗</span></button></section></div></>;
}
